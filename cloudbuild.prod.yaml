steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - builds
      - submit
      - --config=cloudbuild.yaml
      - --substitutions=_ENV=prod,_PROJECT_ID=${PROJECT_ID}

substitutions:
  _ENV: 'prod'