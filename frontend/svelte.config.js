// svelte.config.js — Svelte compiler configuration
//
// This file configures how Svelte compiles your .svelte files into JavaScript.
// The default configuration works well for most projects — we don't need to
// change anything here.
//
// WHY DOES THIS FILE EXIST? Svelte is a compiler, not a runtime library.
// It transforms your template syntax (like {#if}, {#each}, $state) into
// optimized JavaScript that runs in the browser. This config tells the
// compiler how to do that transformation.

/** @type {import("@sveltejs/vite-plugin-svelte").SvelteConfig} */
export default {}
